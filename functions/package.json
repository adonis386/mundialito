{
  "name": "functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions,firestore",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "seed:firestore": "npm run build && node lib/seedFirestore.js",
    "seed:fantasy-demo": "npm run build && node lib/seedFantasyDemo.js",
    "seed:demo-final-scores": "npm run build && node lib/seedDemoFinalScores.js",
    "seed:wc2026-archive": "npm run build && node lib/seedWc2026Archive.js",
    "set-admin": "npm run build && node lib/setAdminClaim.js",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}

